READ ME

Homework Problem 1 2.19: The user uses the definition to determine whether or not the dart
is in the cirlce or out of the cirlce.  If the dart is it in the circle it returns True.
If it is out of the circle it returns False. I ran this program on a Windows machine, using Python 2.7.2 and Wing IDE 101 Version 4.1.

Homework Problem 2: Asks to write a method that takes a string and prints the string in reverse order.
This program does exactly that and it also returns the string as well so as to make it possible to store
the new string in a variable for later use. I ran this program on a Windows machine, using Python 2.7.2 and Wing IDE 101 Version 4.1.

Homework Problem 3: Asks to Write a method that prints a random number between 1 and 6.
this program does exactly that and it also returns the int as well so as to make it possible to store
the new string in a variable for later use. I ran this program on a Windows machine, using Python 2.7.2 and Wing IDE 101 Version 4.1.

Homework Problem 4 2.34: Uses Turtle graphics to draw an image showing the six sides of a dice.
Each side is represented inside a separate square. The program exits immediately after the 
figures have been drawn. I ran this program on a Windows machine, using Python 2.7.2 and Wing IDE 101 Version 4.1.