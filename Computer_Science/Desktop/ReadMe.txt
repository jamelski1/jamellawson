I tested my program by putting an array of different numbers as input.
The program works if the user specifically puts a natural number as the
hailstone number.  It will fail if a regular integer such as -1 or 0 is
put in as input.  It will be stuck in the while loop. I chose the while
loops because it seemed like the easiest way to approach this problem,
and I chose the if, if and else statement because that is what made the
most sense.